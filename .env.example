# Google Gemini 1.5 Flash Vision API Key (Optional environment fallback)
# Note: For public GitHub Pages hosting, users can safely enter their key
# directly in the app's Settings modal, stored securely in browser localStorage.
# NEVER commit your real API key to Git!
VITE_GEMINI_API_KEY=
